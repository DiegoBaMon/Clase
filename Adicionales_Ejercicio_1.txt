# Usamos un bucle para recorrer los números del 1 al 100
for i in range(1, 101):
    # Calculamos el valor descendente correspondiente
    descendente = 101 - i
    # Imprimimos ambos valores en formato de columnas usando f-strings
    print(f"{i:<5} | {descendente:<5}")